import { Text } from "@bacons/react-views";

export function Tweet({ url }: { url: string }) {
  return <Text>Tweet Embed not implemented: {url}</Text>;
}

// Twitter profile embed
export function TwitterProfile({ url }: { url: string }) {
  return <Text>Twitter Profile Embed not implemented: {url}</Text>;
}
